# Shared studio Supabase project. One project for every Reboot prototype; the
# `prototype` column namespaces each game.
#
# This key is deliberately public. It is embedded in the JS bundle on GitHub
# Pages and extractable from any build, so it is committed rather than pretended
# to be a secret. It grants read/insert/update on the levels table and nothing
# else -- no delete, no other table. If it ever needs to stop working, roll it in
# the Supabase dashboard; removing it from this file will not remove it from git
# history. The service_role / secret key never belongs here.
VITE_SUPABASE_URL=https://owgvhhfsnexdgtixalog.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Ui7Q1sm8RlMMRN4HBo0SRw_EFi79y77
